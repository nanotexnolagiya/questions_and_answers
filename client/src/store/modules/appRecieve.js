const state = {
  appRecieve: null
}

const getters = {
  appRecieve: state => state.appRecieve
}

const actions = {}

const mutations = {}

export default {
  state,
  getters,
  actions,
  mutations
}
