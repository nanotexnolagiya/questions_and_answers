const state = {
  appTransfer: null
}

const getters = {
  appTransfer: state => state.appTransfer
}

const actions = {}

const mutations = {}

export default {
  state,
  getters,
  actions,
  mutations
}
